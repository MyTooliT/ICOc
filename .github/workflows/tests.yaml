name: Tests

on:
  - push

jobs:

  linux:

    name: 🐧 Ubuntu
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version:
          - '3.10'

    steps:

    - name: Checkout code
      uses: actions/checkout@v2

    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v2
      with:
        python-version: ${{ matrix.python-version }}

    - name: Install ICOc
      uses: ./.github/actions/install

    - name: Check ICOc code base
      uses: ./.github/actions/check

    - name: Test ICOc
      uses: ./.github/actions/test-unix

  mac:
    name: 🍎 macOS
    runs-on: macos-latest
    strategy:
      matrix:
        python-version:
          - '3.10'

    steps:

    - name: Checkout code
      uses: actions/checkout@v2

    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v2
      with:
        python-version: ${{ matrix.python-version }}

    - name: Install ICOc
      uses: ./.github/actions/install

    - name: Check ICOc code base
      uses: ./.github/actions/check

    - name: Test ICOc
      uses: ./.github/actions/test-unix

  windows:

    name: 🪟 Windows
    runs-on: windows-latest
    strategy:
      matrix:
        python-version:
          - '3.7'
          - '3.8'
          - '3.9'
          # TODO: Reenable Python 3.10 after GitHub deploys a new version of
          #       Python 3.10. See also:
          #       https://github.com/python/mypy/issues/13627

    steps:

    - name: Checkout code
      uses: actions/checkout@v2

    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v2
      with:
        python-version: ${{ matrix.python-version }}

    - name: Install ICOc
      uses: ./.github/actions/install

    - name: Check ICOc code base
      uses: ./.github/actions/check

    - name: Test ICOc
      uses: ./.github/actions/test-windows
